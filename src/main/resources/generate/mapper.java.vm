package ${mapperPackage};

import ${entityImport};
import tk.mybatis.mapper.common.Mapper;

/**
 * ${config.desc}dao层
 *
 * @author ${config.author}
 * @since ${Date}
 */
public interface ${mapper} extends Mapper<${entity}> {

}